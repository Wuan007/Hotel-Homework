// Question 1
// Write the function printInt(n)
// taking one parameter n and print all naturals
// from 1 to n in console.
function printInt(n) {
    //Using a loop print from 1 to n
    // start: 1
    var i = 1;
    // stop: n
    while(i <=n) {
        console.log(i)
        // step: +1
        i++;
}
}

 print(10);   

 // Question 2
 // Write the function printIntRev(n) taking one parameter n and  print all natural numbers in reverse in console (from n to 1)

// from n to 1 in console.
function printInt(n) {
    //Using a loop print from n to 1
    // start: n
    var i = 1;
    // stop: 1
    while(i <=n) {
        console.log(i)
        // step: +1
        i++;

// Question 3
        // Write the function checkInput(x) taking one parameter x and return the string ‘number’ if x is a number; return the string ‘string’ if x is a string; and return ‘boolean’ if x is a boolean. Otherwise returns -1. 
Examples:
       checkInput(5) ====> ‘number’
       checkInput(‘hello’)======> ‘string’
       checkInput(‘5’) ======> ‘string’
       checkInput(true)=====> boolean
       checkInput([1,2,3,4]) =====> -1



       // Question 4
       //Write the function SimpleEvenAdding (num)
       //taking a number and add up all the even numbers 
       // from 1 to num.
       function simpleEvenAdding (num){
      // store the answer 
      var soluction = ...;
      // loop over numbers from 1 to num
       for(...;...;...){
          // checck if current index is even or odd
      }   if(...){

      }
    }
    return soluction;

    // Question 5
// Write the function letterCapitalize(str)
// taking a string and capitalize the first letter
// of each word. The given words will be 
// seperated by only one space.
function letterCapitalize(str){
    // seperate the str into array of words
    // using split

    // loop over the array of words

    // capitalize the first character of each word
}

// Question 6

// Write the function simpleReverse(str) taking a string and return the string in reversed order.
// simpleReverse("hello")======>"olleh"
// simpleReverse("I love Code")======> 'edoC evoL I'


// Question 7

// Write the function findDiff(arr) taking an array of numbers as parameters and return the difference between the maximum number and the minimum number (max-min).

function findDiff(arr) {
    // find min array
    >Math.min(1,2);
    <1

    // max-min array
    > Math.max(34,56,78,90,1.5);
    <90
}

